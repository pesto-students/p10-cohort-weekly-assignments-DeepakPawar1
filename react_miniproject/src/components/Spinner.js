// Spinner.js

import React from 'react';
import "./Spinner.css";

const Spinner = () => {
  return (
  <div className="spinnerContainer">
  <div className="Spinner" />
  </div>
  );

};

export default Spinner;
